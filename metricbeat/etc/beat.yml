metricbeat:
  modules:
    #beat:
    #  metrics:
    #    beat:
    #      period: 10s
    elasticsearch:
      hosts:
        - "http://localhost:9200"
        - "http://localhost:9200"
      metrics:
        info:
          period: 10s
        cluster-stats:
          period: 10s
        stats:
          period: 10s
          mertric-specifcs: "12"
    #exec:
    #  metrics:
    #    exec:
    #      commands:
    #        - "ping 127.0.0.1"
    #        - "echo 'test'"
    redis:
      metrics:
        info:
          period: 1s
